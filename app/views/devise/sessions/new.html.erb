<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="https://fonts.googleapis.com/css?family=Roboto:400,700" rel="stylesheet">
<title>Bootstrap Sign up Form Horizontal</title>
<link rel="stylesheet" href="/assets/signup.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script> 


</head>
<body>

<div class="signup-form">
 <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
   <div style="text-align: center;">
			<h2>Sign In</h2>
		</div>		
        <div class="centre-align"><img src="/assets/onshoplogo-v2.png"  class="logo" alt="On Shop"></div>
        <div class="screen-1">
          <div class="email">
            <label for="email">Email Address</label>
            <div class="sec-2">
              <ion-icon name="mail-outline"></ion-icon>
              <%= f.email_field :email, class: "input-box", autofocus: true %>
            </div>
          </div>
            <div class="password">
            <label for="password">Password</label>
            <div class="sec-2">
              <ion-icon name="lock-closed-outline"></ion-icon>
              <%= f.password_field :password,class: "input-box", autocomplete: "off" %>
              <ion-icon class="show-hide" name="eye-outline"></ion-icon>
            </div>
          </div>
            <div class="field mt-3">
              <% if devise_mapping.rememberable? -%>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              <% end -%>
              <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                <span style="margin-left:220px;"><%= link_to "Forgot your password?", new_password_path(resource_name) %></span>
              <% end %>
            </div></br>

          <div class="centre-align"><%= f.submit "Log in",class: "login btn btn-primary"%></div></br>
          <div class="footer mt-3 mb-3">
            <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
              <p>New OnShoppers?
              <%= link_to  "Sign up here!", new_registration_path(resource_name)%></p>
            <% end %>           
          </div>
        </div>
    <% end %>
</div>
</body>
</html>