<!DOCTYPE html>
<html>
  <head>
    <title>Mall</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="navbar.css">
    <style>
        .navbar {
          margin: auto;
        }
    </style>
</head>


  <nav class="navbar custom-navbar navbar-expand-lg bg-dark" >
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
    <img src="../assets/onshoplogo.png" style="height:60px" alt="On Shop">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" style="color:#EAFDFC" href="<%= url_for(stores_path) %>">Shop</a>
          </li>
          <li class="nav-item">
          <a class="nav-link" style="color:#EAFDFC" href=user_cart_path(current_user)>Cart</a>
          </li>
          <li class="nav-item">
          <a class="nav-link" style="color:#EAFDFC" href=>Log Out</a>
        </li>
        </ul>
      </div>
    </div>


  </div>
</nav>

  <body>
    <% if notice %> <p class="alert alert-success"><%= notice %></p> <% end %> <% if alert %> <p class="alert alert-danger"><%= alert %></p> <% end %>
    
    <% if user_signed_in? %>
    logged in user: <%= link_to current_user.name,user_path(current_user) %></br>
    <%= link_to "Log Out", log_out_path, method: :delete %>
    <% end %>


    <%= yield %>
  </body>
</html>
